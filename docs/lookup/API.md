# API Contract Registry

> **LAW**: This file is updated BEFORE the code changes. The signature here is the truth.
> Format: `METHOD /route {body} -> ResponseModel — plain English description`

---

## Library Routes
*Location: `src/v3engine/api/routes/library.py`*

*(No routes defined yet — add them here before implementation)*

---

## Audio Routes
*Location: `src/v3engine/api/routes/audio.py`*

*(No routes defined yet — add them here before implementation)*

---

## Identity Routes
*Location: `src/v3engine/api/routes/identities.py`*

*(No routes defined yet — add them here before implementation)*
